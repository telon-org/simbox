# SDD: Multi-SIM Programming - Specifications

**Дата**: 2026-03-03  
**Статус**: DRAFT

---

## Technical Specifications

### set_ki.php

**Path**: `system/set_ki.php`

```php
#!/usr/bin/php
<?php
include("/usr/simbox/modules/at.php");

function supersim_make_smsp($smsp) {
    if (strlen($smsp) != 24) { echo("Incorrect smsp Length!"); die(); }
    return $smsp;
}

function supersim_make_ki($ki) {
    if (strlen($ki) != 32) { echo("Incorrect KI Length!"); die(); }
    return $ki;
}

function supersim_make_imsi($imsi) {
    if (strlen($imsi) != 18) { echo("Incorrect IMSI Length!"); die(); }
    
    $a = str_split($imsi);
    return $a[1].$a[0].$a[3].$a[2].$a[5].$a[4].$a[7].$a[6].
           $a[9].$a[8].$a[11].$a[10].$a[13].$a[12].
           $a[15].$a[14].$a[17].$a[16];
}

function supersim_make_iccid($iccid) {
    if (strlen($iccid) == 19) $iccid .= "F";
    if (strlen($iccid) != 20) { echo("Incorrect ICCID Length!"); die(); }
    
    $a = str_split($iccid);
    return $a[1].$a[0].$a[3].$a[2].$a[5].$a[4].$a[7].$a[6].
           $a[9].$a[8].$a[11].$a[10].$a[13].$a[12].
           $a[15].$a[14].$a[17].$a[16].$a[19].$a[18];
}

$adapter = $argv[1];   // 9 or 16
$dongle = $argv[2];
$dat_ki = fulltrim($argv[3]);
$dat_imsi = fulltrim($argv[4]);
$dat_iccid = fulltrim($argv[5]);
$dat_smsc = fulltrim($argv[6]);
$imei = fulltrim($argv[7]);

if ($adapter == "9") {
    // Type 9 STK programming
    exec_at_command($dongle, 'AT^STGI=0,0');
    exec_at_command($dongle, 'AT^STGR=0,0,1,22');
    exec_at_command($dongle, 'AT^STGR=3,1,1,"' . bin2hex($dat_ki) . '"');
    exec_at_command($dongle, 'AT^STGR=3,1,1,"' . bin2hex($dat_imsi) . '"');
    exec_at_command($dongle, 'AT^STGR=3,1,1,"' . bin2hex($dat_iccid) . '"');
    exec_at_command($dongle, 'AT^STGR=3,1,1,"' . bin2hex($dat_smsc) . '"');
    // ... select profile
    // ... reboot
}

if ($adapter == "16") {
    // Type 16 SuperSIM programming
    // ... CSIM APDU commands
}
?>
```

### new_ki.php

**Path**: `system/new_ki.php`

```php
#!/usr/bin/php
<?php
// Fetch KI from central server
$ki_data = file_get_contents("http://simserver:8122/new_ki.php");

// Parse
preg_match('/KI:([A-F0-9]{32})/', $ki_data, $ki_match);
preg_match('/IMSI:(\d{15,18})/', $ki_data, $imsi_match);
preg_match('/ICCID:(\d{19,20})/', $ki_data, $iccid_match);
preg_match('/SMSC:(\d{20,24})/', $ki_data, $smsc_match);

// Call set_ki.php
$cmd = '/usr/simbox/system/set_ki.php "9" "' . $dongle . '" "'
     . $ki_match[1] . '" "' . $imsi_match[1] . '" "' 
     . $iccid_match[1] . '" "' . $smsc_match[1] . '" "' . $imei . '"';
system($cmd);
?>
```

---

## File Index

| File | Purpose | Lines |
|------|---------|-------|
| system/set_ki.php | KI programming script | ~207 |
| system/new_ki.php | KI fetch from server | ~100 |
| modules/at.php | AT command wrapper | ~150 |
| www/simbox/sim.php | Web interface | ~2233 |

---

*Generated by /legacy analysis - 2026-03-03*
